# Comunidad V2 — implementación consolidada

Se conserva la tabla CMS `public.projects` existente. Los proyectos publicados por usuarios viven en `public.community_projects` para evitar colisiones de esquema.

Incluye perfiles, follows, comentarios, proyectos de comunidad, reacciones y publicación persistente.
