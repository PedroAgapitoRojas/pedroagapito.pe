/* Edita SOLO este valor cuando publiques el runner.
   En local puedes dejar http://localhost:3000/api/execute.
   Nunca pongas aquí una service_role key de Supabase. */
window.PEDRO_EXECUTION = Object.freeze({
  endpoint: 'http://localhost:3000/api/execute'
});
