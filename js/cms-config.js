/* =========================================================
   CMS REMOTO (SUPABASE)
   ---------------------------------------------------------
   La web funciona aunque esto quede desactivado.
   Cuando tengas tu proyecto Supabase, completa URL + anonKey
   y cambia enabled a true. El contenido local seguirá siendo
   el respaldo si el servicio no responde.
========================================================= */
window.portfolioCMS = {
  enabled: false,
  supabaseUrl: "",
  anonKey: ""
};
